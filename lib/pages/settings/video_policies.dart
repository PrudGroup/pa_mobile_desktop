import 'package:flutter/material.dart';
    
class VideoPolicies extends StatelessWidget {

  const VideoPolicies({ super.key });
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}