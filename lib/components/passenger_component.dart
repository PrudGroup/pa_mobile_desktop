import 'package:flutter/material.dart';
import 'package:prudapp/models/bus_models.dart';

class PassengerComponent extends StatelessWidget {
  final PassengerDetail passenger;
  const PassengerComponent({super.key, required this.passenger});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
